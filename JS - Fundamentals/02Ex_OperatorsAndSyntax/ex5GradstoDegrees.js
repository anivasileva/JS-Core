function convertor(grad) {
    let degrees = grad % 400 * 0.9;
    let result;
    if (grad < 0) {
        console.log(result = 360 + degrees);
    }
    else {
        console.log(degrees);
    }
}

convertor(850);